apiVersion: v1
kind: Service
metadata:
  name: {{ include "nim-llm.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nim-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nim-llm.selectorLabels" . | nindent 4 }}
